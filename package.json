{
  "name": "crime-intelligence-platform-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "install": "npm install --prefix backend && npm install --prefix frontend",
    "build": "npm run build --prefix frontend && NODE_OPTIONS='--max-old-space-size=4096' npm run build --prefix backend",
    "start": "NODE_ENV=production npm start --prefix backend"
  },
  "dependencies": {
    "zcatalyst-sdk-node": "^3.4.0"
  }
}
